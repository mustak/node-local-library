{% extends "layouts/default.njk" %}

{% block main %}
  <h1>{{message}}</h1>
  <h2>{{error.status}}</h2>
  <pre>{{error.stack}}</pre>
{% endblock main %}

extends layout

block content
  h1= message
  h2= error.status
  pre #{error.stack}
